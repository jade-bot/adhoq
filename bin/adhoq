#!/usr/bin/env node

var cli = require('../cli'),
    program = require('commander'),
    package = require('../package')

program
  .usage('...')
  .version(package.version)
  .parse(process.argv)
  
cli.process(program)
